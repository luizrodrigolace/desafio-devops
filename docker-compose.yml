services:
  app:
    build: .
    ports:
      - "8000:8000"
#    environment:
#      - NAME=${NAME}
    volumes:
      - .:/app  # Bind mount: pasta atual do host -> /app no container
    networks:
      - desafio-devops

networks:
  desafio-devops:
    driver: bridge